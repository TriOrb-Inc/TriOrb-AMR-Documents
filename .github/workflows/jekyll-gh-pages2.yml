name: Legacy Jekyll deployment (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is kept for history and intentionally disabled."
